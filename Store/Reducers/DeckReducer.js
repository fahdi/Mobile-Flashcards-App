import Types from "../const/Types"

const initState = {

}

const deckReducer = (state = initState, action) => {
    switch(action.type){
        default: 
        return state;
    }
}
export default deckReducer;